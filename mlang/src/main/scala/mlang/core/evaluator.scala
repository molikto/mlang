package mlang.core


import mlang.syntax._


protected def evalRec(vs: Seq[(String, Term)]) given (c: Context): Map[String, Value] = {
  ???
}

protected def eval(term: Term) given (c: Context): Value = {
  ???
}


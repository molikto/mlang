

nat = [zero, succ(nat)];

bool = [true, false];

odd(a: nat): bool;

even(a: nat): bool = match a {
  zero -> bool:true;
  succ(a) -> odd(a);
};

odd(a: nat): bool = match a {
  zero -> bool:false;
  succ(a) -> even(a);
};
